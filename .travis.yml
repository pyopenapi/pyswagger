language: python
python:
    - "2.6"
    - "2.7"
install:
    - pip install -r requirements-dev.txt
    - pip install coveralls
script:
    - py.test --cov=pyswagger pyswagger/tests
after_success:
    - coveralls

